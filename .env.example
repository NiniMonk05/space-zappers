# LNbits Configuration for Space Zappers
# These are embedded at build time (Vite)

# LNbits URL - use /lnbits to proxy through Nginx (avoids CORS issues)
# Configure Nginx to proxy /lnbits/* to your LNbits instance
VITE_LNBITS_URL=/lnbits

# Invoice/Read key from LNbits wallet (safe to expose - can only create invoices and check status)
VITE_LNBITS_INVOICE_KEY=your_invoice_key_here
